{
  "name": "@harnessflow-demo/writeonce",
  "private": true,
  "version": "0.1.0",
  "description": "HarnessFlow v0.1.0 quickstart demo: Markdown -> Medium walking-skeleton publisher (illustration only, not a production tool).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run --reporter=verbose"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
