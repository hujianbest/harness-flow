# 分诊标签

这些技能使用五种规范分诊角色。本文件将这些角色映射到此仓库议题跟踪器中实际使用的标签字符串。

| mattpocock/skills 中的标签 | 本跟踪器中的标签 | 含义 |
| -------------------------- | -------------------- | ---- |
| `needs-triage`             | `needs-triage`       | 维护者需要评估此议题 |
| `needs-info`               | `needs-info`         | 等待报告者提供更多信息 |
| `ready-for-agent`          | `ready-for-agent`    | 规格完整，可交给离线智能体 |
| `ready-for-human`          | `ready-for-human`    | 需要人工实现 |
| `wontfix`                  | `wontfix`            | 不会采取行动 |

当技能提及某个角色时（例如“应用可交给离线智能体的分诊标签”），使用此表中对应的标签字符串。

编辑右侧列，使其与你实际使用的词汇一致。
